# The directory Mix will write compiled artifacts to.
/_build/
/deps

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log

# Dependency directories
node_modules/

# Phoenix static assets (rebuild on Fly/CI)
/priv/static/assets/
/priv/static/cache_manifest.json

# Generated sitemap files (regenerated daily)
/priv/static/sitemaps/

# User uploads (stored in Cloudflare R2 in production, local only in dev)
/priv/static/uploads/

# Compiled theme files with hashes (generated by mix assets.deploy)
/priv/static/themes/*-*.css
/priv/static/themes/*-*.css.gz
/priv/static/themes/*.css.gz

# Compiled image files with hashes (generated by mix assets.deploy)
/priv/static/images/**/*-*.png
/priv/static/images/**/*-*.jpg
/priv/static/images/**/*-*.jpeg
/priv/static/images/**/*-*.gif
/priv/static/images/**/*-*.webp
/priv/static/images/**/*-*.svg

# Coverage reports
cover/

# Environment variables
.env

# Deployment configuration (may contain secrets)
fly.toml

# Cookie files (can contain session/auth tokens)
cookies.txt
*.cookiejar
*.cookies
curl_cookies.txt

# MCP configuration with API keys
.mcp.json

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specific
.DS_Store

# AI Assistant Configuration Files (not needed in version control)
.clinerules/
.cursor/
.github/instructions/
.roo/
.roomodes
.taskmaster/
.trae/
.windsurf/
.claude/commands/
.claude/agents/

# Playwright test results
test-results/

# Playwright MCP screenshots and artifacts
.playwright-mcp/

# Task files
# tasks.json
# tasks/
erl_crash.dump

# Clerk migration scripts with sensitive data (password hashes, PII)
scripts/clerk-migration/users.json
scripts/clerk-migration/import-results-*.json
scripts/clerk-migration/node_modules/

# Database schema dumps (generated ad-hoc, not the tracked structure.sql)
# Note: priv/repo/structure.sql is tracked for deployment
/*.sql
/scripts/**/*.sql

# Database backups (contains sensitive data, stored locally only)
/backups/
*.dump

# Production database dumps (contains sensitive production data!)
/priv/dumps/

# Performance baselines (development artifacts, not needed in version control)
.baselines/
