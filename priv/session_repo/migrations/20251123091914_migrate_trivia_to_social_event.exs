defmodule EventasaurusApp.SessionRepo.Migrations.MigrateTriviaToSocialEvent do
  use Ecto.Migration

  # SessionRepo doesn't have sources table, this migration is only for main Repo
  def change do
    :ok
  end
end
