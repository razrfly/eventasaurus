# Task ID: 2
# Title: Design and Implement Ticket Schema and Migration
# Status: done
# Dependencies: 1
# Priority: high
# Description: Create the new Ticket data model and corresponding database migration.
# Details:
Define Ticket schema with fields: id, event_id (FK), title, description, price_cents, currency (default: usd), quantity, quantity_sold, starts_at, ends_at, tippable (boolean, default: false), created_at, updated_at. Generate and run Ecto migration. Add validation for price, quantity, and availability window. Use Ecto 3.10+ for schema and migration.

# Test Strategy:
Write unit tests for schema validation. Test migration rollback and forward. Verify constraints (e.g., price_cents > 0, quantity >= quantity_sold).
